/** Realm-wide spell archive (Arcanum / global lists). */
export const REALM_SPELLBOOK_PAGE_SIZE = 10;

/** Character spellbook tab (more spells per page). */
export const CHARACTER_SPELLBOOK_PAGE_SIZE = 20;
